{
  "name": "@frctl/twig-example",
  "version": "0.2.11",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "fractal start --sync",
    "build": "fractal build"
  },
  "dependencies": {
    "@frctl/fractal": "^1.5.15",
    "@frctl/mandelbrot": "^1.10.3",
    "@frctl/twig": "^1.2.13"
  },
  "devDependencies": {
    "node-fetch": "^2.6.1",
    "@frctl/adapter-tests": "workspace:../adapter-tests"
  }
}
